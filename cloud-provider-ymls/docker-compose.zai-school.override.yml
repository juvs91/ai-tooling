services:
  proxy_cloud_school:
    image: ghcr.io/1rgs/claude-code-proxy:main
    container_name: ai-tooling-proxy_cloud-1-school
    restart: unless-stopped
    env_file:
      - ./.env
      - profile-envs/cloud.zai-anthropic.env
    environment:
      - PYTHONUNBUFFERED=1
      - UV_LINK_MODE=copy
      - UV_PROJECT_ENVIRONMENT=/tmp/.venv-school
    ports:
      - "8084:8082"
    volumes:
      - ./vendor/claude-code-proxy:/claude-code-proxy
    command: uv run uvicorn server:app --host 0.0.0.0 --port 8082
