#!/usr/bin/env bash
# Wrapper that makes scripts/cc-proxy-up available as a global command.
# Keeps the actual implementation in the repository under scripts/.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$(realpath "${BASH_SOURCE[0]}")")" && pwd)"
exec "${SCRIPT_DIR}/../scripts/cc-proxy-up" "$@"
